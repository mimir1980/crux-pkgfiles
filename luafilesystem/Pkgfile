# Description: File System for the Lua Programming Language
# URL: http://www.keplerproject.org/luafilesystem/
# Packager: Roosevelt knowledgeispower80 at gmail dot com
# Depends on: lua51filesystem

name=luafilesystem
version=1.6.2
release=1
source=(http://github.com/downloads/keplerproject/$name/$name-$version.tar.gz)

build() {
	cd $name-$version
make
make PREFIX=/usr LUA_LIBDIR=/usr/lib/lua/5.2 install
install -Dm644 src/lfs.so $PKG/usr/lib/lua/5.2/lfs.so
install -Dm644 src/lfs.h $PKG/usr/include/5.2/lfs.h
}
